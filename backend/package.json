{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "dev:all": "concurrently --kill-others --names \"PHP,FRONT,QUEUE\" \"php artisan serve\" \"npm --prefix ../frontend run dev -- --host --port 5173\" \"php artisan queue:work redis --queue=default,heartbeat,work --tries=1 --verbose\" \"php artisan schedule:work --seconds=2 --verbose\"",
        "queue:work": "php artisan queue:work redis --queue=default,heartbeat,work --tries=1 --verbose"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.13.2",
        "concurrently": "^9.2.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
